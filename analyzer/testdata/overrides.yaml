# Error types for test
---
pointer:
  - test/a.PointerOverrideError

value:
  - test/a.ValueOverrideError

suppress:
  - test/a.SuppressOverrideError

wrappers:
  is:
    - test/wrappers/vars.Is
    - (test/wrappers/iface.Suite).Is
  as:
    - test/wrappers/vars.As
    - (test/wrappers/iface.Suite).As
